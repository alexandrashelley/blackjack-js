const Deck = require("../lib/Deck");

describe("the Deck object", () => {

  it("A deck is initialised with with an array", () => {
    const deck = new Deck();
    expect(deck.deck).toEqual([]);
  });

  it("A deck is initialised with an array", () => {
    const deck = new Deck();
    expect(deck.deck).toEqual([]);
  });
});
